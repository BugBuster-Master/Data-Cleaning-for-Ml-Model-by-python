# Data-Cleaning-for-Ml-Model-by-python
This repository contains scripts for cleaning and preprocessing datasets before using them in machine learning models. It handles missing values, removes duplicates, explores dataset statistics, and ensures data quality to prepare data for analysis and modeling.



# Data-Cleaning-Pipeline

## Overview
This repository contains scripts for cleaning and preprocessing datasets before feeding them into machine learning models. It handles missing values, removes duplicates, explores dataset statistics, and ensures data quality to prepare data for analysis and modeling.

## Features
- Load datasets using pandas
- Display sample rows and dataset shape
- View dataset info and statistics
- Detect missing and duplicated values
- Ready-to-use for machine learning pipelines

## Usage
1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/Data-Cleaning-Pipeline.git
